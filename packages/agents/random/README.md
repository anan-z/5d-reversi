# packages/agents/random

**Status: not yet implemented.** Build via `docs/roadmap/build-steps.md`
Step 8.

Reference `PlayerAgent` implementation: picks uniformly from
`legalMoves`. Exists both as a sanity-check opponent and as the minimal
worked example of the protocol end-to-end — see the example in
`docs/protocol/playeragent-spec.md` §5.
